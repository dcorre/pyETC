coverage:
  precision: 2
  range: 50..100
  round: nearest
  status:
    project:
      default:
        target: auto
        threshold: 1
    patch:
      default:
        target: 90

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: no
